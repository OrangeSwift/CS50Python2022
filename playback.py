#SDG

# Request User Input
userInput=input("Input: ")
# Copy userInput to a new variable and change spaces in string to ...
userInputChanged=userInput.replace(" ", "...")
# Print input with changes
print(userInputChanged)
